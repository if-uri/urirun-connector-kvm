# TODO
- [ ] Add text-typing and mouse-click routes.
- [ ] Add a backend abstraction (xdotool / ydotool / pyautogui) selected by env.
- [ ] Add an Xvfb-backed integration test gated behind an `--online` flag.
- [ ] Promote the hub catalog entry from `planned` to `available` once published.
